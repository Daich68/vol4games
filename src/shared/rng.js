// Детерминированный псевдо-рандом (одно число → одно число в [0,1)).
// Удобно для процедурной графики: одинаковый seed → одинаковая картинка.
export function pseudoRand(n) {
  const s = Math.sin(n * 12.9898 + 78.233) * 43758.5453;
  return s - Math.floor(s);
}
